---
import HeroGraph from './HeroGraph.astro';
import { SITE } from '../lib/site';

const version = SITE.latestVersion;
---

<section class="grain relative overflow-hidden" data-commit-root>
  <div
    aria-hidden="true"
    class="pointer-events-none absolute inset-0"
    style="background:
      radial-gradient(60% 50% at 18% 0%, hsl(var(--primary) / 0.22), transparent 70%),
      radial-gradient(40% 30% at 90% 20%, hsl(var(--graph-3) / 0.08), transparent 70%),
      linear-gradient(to bottom, transparent 60%, hsl(var(--background)) 100%);"
  ></div>
  <div
    aria-hidden="true"
    class="pointer-events-none absolute inset-x-0 bottom-0 h-px"
    style="background: linear-gradient(to right, transparent, hsl(var(--primary) / 0.5) 30%, hsl(var(--primary) / 0.5) 70%, transparent);"
  ></div>

  <div class="container-site rail-offset relative grid items-center gap-14 pb-20 pt-16 lg:grid-cols-[1.05fr_0.95fr] lg:pb-28 lg:pt-24">
    <div class="min-w-0 max-w-2xl">
      <h1 class="display text-[2.9rem] leading-[0.98] sm:text-7xl">
        <span class="mb-5 block font-mono font-medium text-xs uppercase tracking-[0.18em] text-primary">AngKorGit · Git 客户端 for macOS, Windows, Linux</span>
        一个为<em>日常</em>工作打造的原生 Git 客户端。
      </h1>
      <p class="mt-7 max-w-xl text-lg leading-relaxed text-muted">
        I got tired of Git 客户端s that were secretly browsers. This one is native, Rust and libgit2 underneath, and it
        does the things you do every day without asking for an account or a subscription first.
      </p>
      <div class="mt-9 flex flex-col items-start gap-3 sm:flex-row sm:items-center">
        <a
          href="#install"
          class="group inline-flex items-center gap-2 rounded-md bg-primary px-5 py-3 text-sm font-medium text-primary-foreground shadow-soft transition-all hover:opacity-90 hover:shadow-none"
        >
          Download {version}
          <svg class="h-4 w-4 transition-transform group-hover:translate-y-0.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M12 5v14m0 0 6-6m-6 6-6-6" />
          </svg>
        </a>
        <a href={SITE.repo} target="_blank" rel="noreferrer" class="link-underline px-1 py-3 text-sm text-muted hover:text-foreground">
          Read the source on GitHub
        </a>
      </div>
      <p class="mt-8 font-mono text-xs text-faint">免费。MIT 许可。无遥测。自动更新。</p>
    </div>

    <div class="relative min-w-0" data-reveal style={{ '--reveal-delay': '160ms' }}>
      <HeroGraph />
    </div>
  </div>
</section>
