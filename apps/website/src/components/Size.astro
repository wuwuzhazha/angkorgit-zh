---
import Commit from './Commit.astro';
---

<Commit id="size" lane={1} class="py-20 lg:py-28">
  <div class="container-site rail-offset">
    <div class="max-w-2xl" data-reveal>
      <p class="font-mono text-xs uppercase tracking-[0.18em] text-primary">原生</p>
      <h2 class="display mt-3 text-4xl leading-[1.02] sm:text-5xl">原生，而不是<em>浏览器</em>。</h2>
      <p class="mt-6 text-[17px] leading-relaxed text-muted">
        大多数 Git 客户端本质上是套着自己浏览器外壳的网页，所以打开要等一会儿，
        而且很占磁盘。AngKorGit 用操作系统自带的 webview 绘制窗口，并通过
        libgit2 在同一进程中访问仓库。它打开快、自行更新、不碍事。
      </p>
    </div>

    <dl class="mt-14 grid max-w-3xl gap-6 border-t border-border-subtle pt-8 sm:grid-cols-3" data-reveal style={{ '--reveal-delay': '120ms' }}>
      <div>
        <dt class="font-mono text-xs uppercase tracking-[0.14em] text-faint">引擎</dt>
        <dd class="mt-2 text-[15px] leading-snug text-foreground">Rust 和 libgit2，日常操作不外调命令行。</dd>
      </div>
      <div>
        <dt class="font-mono text-xs uppercase tracking-[0.14em] text-faint">10 万次提交</dt>
        <dd class="mt-2 text-[15px] leading-snug text-foreground">提交图与 diff 均已虚拟化，超长历史也能流畅滚动。</dd>
      </div>
      <div>
        <dt class="font-mono text-xs uppercase tracking-[0.14em] text-faint">冷启动</dt>
        <dd class="mt-2 text-[15px] leading-snug text-foreground">打开、启动画面、仓库。仓库在进程内读取，无需等待守护进程。</dd>
      </div>
    </dl>
  </div>
</Commit>
