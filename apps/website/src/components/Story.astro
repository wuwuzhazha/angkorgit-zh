---
import Commit from './Commit.astro';
---

<Commit id="why" class="py-20 lg:py-28">
  <div class="container-site rail-offset">
    <div class="grid gap-10 lg:grid-cols-[minmax(0,1fr)_minmax(0,1.4fr)]" data-reveal>
      <div class="min-w-0">
        <p class="font-mono text-xs uppercase tracking-[0.18em] text-primary">init</p>
        <h2 class="display mt-3 text-4xl leading-[1.02] sm:text-5xl">为什么做这个</h2>
      </div>
      <div class="space-y-5 text-[17px] leading-relaxed text-muted">
        <p>
          我整天都在用 Git，用着我喜欢的 Mac，试过的每个客户端都像穿了外套的网页。打开慢、
          占磁盘、第一次提交前还要先登录。命令行更快，但一张好的
          提交图和像样的冲突视图是值得拥有的。
        </p>
        <p>
          于是在 2026 年 8 月我开始写一个。引擎是 Rust 之上的 libgit2，窗口用系统
          webview，收录功能的规则很简单：如果 GitKraken 有一百个功能，我只要其中二十五个
          真正要紧的，并且做到位。
        </p>
        <p>
          本页其余部分像历史一样排布：最新提交在图顶，越往下越旧，
          滚动时左侧的线随阅读进度填充。
        </p>
      </div>
    </div>
  </div>
</Commit>
